# TASKS

Use this board to coordinate manual handoff between planner, implementer, and reviewer.

Status values:
- `todo`
- `in_planning`
- `ready_for_implement`
- `in_implementation`
- `ready_for_review`
- `in_review`
- `changes_requested`
- `done`
- `blocked`

| Task ID | Scope | Planner Agent | Implementer Agent | Reviewer Agent | Status | Acceptance Criteria | Evidence | Next Role |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| T-001 | Add release-please configuration | claude | codex | claude | done | `release-please-config.json` exists with `release-type: maven`, changelog-sections mapping, excluded types. `.release-please-manifest.json` exists with `{ ".": "0.6.4" }`. Both files are valid JSON. | `release-please-config.json`, `.release-please-manifest.json`; `mvn -q spotless:apply`; `mvn -q -DskipTests test-compile`; `mvn -T 1C -q test` | — |
| T-002 | Add release-please GitHub Action in ci.yml | claude | codex | claude | done | `ci.yml` contains a `release-please` job using `googleapis/release-please-action@v4`, runs on `main` push after `build`, outputs `release_created` and `tag_name`. | `.github/workflows/ci.yml`; `mvn -q spotless:apply`; `mvn -q -DskipTests test-compile`; `mvn -T 1C -q test` | — |
| T-003 | Restructure CI pipeline | claude | codex | claude | done | `ci.yml` has no `tag-version` or `release` jobs. New `publish` job runs only when `release_created == 'true'`, publishes to Maven Central via `mvn -B -Prelease -DskipTests deploy`. `build` and `sonar` jobs unchanged. | `.github/workflows/ci.yml`; `mvn -q spotless:apply`; `mvn -q -DskipTests test-compile`; `mvn -T 1C -q test` (passed on retry after transient Ryuk failure) | — |
| T-004 | Remove custom release scripts | claude | codex | claude | done | `scripts/ai-release.sh` and `scripts/compose-release-notes.sh` are deleted. | `scripts/ai-release.sh` deleted; `scripts/compose-release-notes.sh` deleted; `mvn -q spotless:apply`; `mvn -q -DskipTests test-compile`; `mvn -T 1C -q test` | — |
| T-005 | Simplify `scripts/ai-pr.sh sync` | claude | codex | claude | done | PR body generated by `sync` no longer contains "Release Notes", "Type of Change", or "Scope by Commit Type" sections. Retains: Summary, Breaking Changes, Included Commits, Test Plan. Dead helper functions removed. | `scripts/ai-pr.sh`; `mvn -q spotless:apply`; `mvn -q -DskipTests test-compile`; `mvn -T 1C -q test` | — |
| T-006 | Simplify PR template | claude | codex | claude | done | `.github/PULL_REQUEST_TEMPLATE.md` has no "Type of Change" checklist or "Release Notes" section. Retains: Summary, Breaking Changes, Test Plan. | `.github/PULL_REQUEST_TEMPLATE.md`; `mvn -q spotless:apply`; `mvn -q -DskipTests test-compile`; `mvn -T 1C -q test` | — |
| T-007 | Remove label-based release configs | claude | codex | claude | done | `.github/release-drafter.yml` and `.github/release.yml` are deleted. | `.github/release-drafter.yml` deleted; `.github/release.yml` deleted; `mvn -q spotless:apply`; `mvn -q -DskipTests test-compile`; `mvn -T 1C -q test` | — |
| T-008 | Remove `central.skipPublishing` mechanism | claude | codex | claude | done | `pom.xml` has no `<central.skipPublishing>` property and no `<skipPublishing>` in plugin config. Central-publishing plugin and rest of `release` profile remain intact. | `pom.xml`; `mvn -q spotless:apply`; `mvn -q -DskipTests test-compile`; `mvn -T 1C -q test` | — |
| T-009 | Update `CLAUDE.md` | claude | codex | claude | done | "Release Rules", "Release Safety", "CI Pipeline", "PR policy" sections reflect release-please model. No references to `ai-release.sh`, `compose-release-notes.sh`, `central.skipPublishing`, or two-phase prepare/finalize. | `CLAUDE.md`; `mvn -q spotless:apply`; `mvn -q -DskipTests test-compile`; `mvn -T 1C -q test` | — |
| T-010 | Bootstrap CHANGELOG.md | claude | codex | claude | done | `CHANGELOG.md` exists with bootstrap note pointing to prior GitHub Releases. | `CHANGELOG.md`; `mvn -q spotless:apply`; `mvn -q -DskipTests test-compile`; `mvn -T 1C -q test` | — |
